let lavariable // no dejar a las variables sin inicializar porque estamos usando any hay que asignar el valor

lavariable = "chanchito feliz"
lavariable = 12


function chanchitoFeliz(config:any) {
    return config
} 